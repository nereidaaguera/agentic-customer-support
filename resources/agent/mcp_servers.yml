# MCP Servers
#
# Databricks apps that provide MCP (Model Context Protocol) servers
# for agent tools and capabilities

resources:
  apps:
    outage_info_mcp_server:
      name: "outage-info-mcp-server-${bundle.target}"
      description: "MCP Server - Telco outage info"
      source_code_path: ../../telco_support_agent/mcp_servers/outage_info_server